services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_API_URL
        - VITE_WS_URL
        - VITE_BASE_URL
    restart: unless-stopped
    ports:
      - "80:80"
